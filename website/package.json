{
  "name": "durably-website",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:llms": "node scripts/generate-llms.js",
    "dev": "pnpm generate:llms && vitepress dev",
    "build": "pnpm generate:llms && vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "@resvg/resvg-js": "2.6.2",
    "satori": "0.18.3",
    "vitepress": "^1.6.4"
  }
}
