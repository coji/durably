pre-commit:
  parallel: true
  jobs:
    - name: oxfmt
      glob: '*.{js,ts,jsx,tsx,json,css,md,yml,yaml}'
      exclude:
        - pnpm-lock.yaml
        - CHANGELOG.md
      run: pnpm exec oxfmt --write {staged_files} && git add {staged_files}
